package DSPractice;

public class GoodNode {
	//Q: Given a graph of nodes, decide how many minimum moves must be made in order to make
	//   each node a "good" node
	//  A good node is one that is either the (1) the tail node, (2) pointing to tail node, (3) or pointing a good node
}
